{
  "name": "image-editor",
  "description": "Open images in an editor",
  "repository": "https://github.com/asiloisad/pulsar-image-editor",
  "version": "0.6.1",
  "license": "MIT",
  "engines": {
    "atom": "*"
  },
  "main": "./lib/main",
  "consumedServices": {
    "status-bar": {
      "versions": {
        "^1.0.0": "consumeStatusBar"
      }
    },
    "tree-view": {
      "versions": {
        "^1.0.0": "consumeTreeView"
      }
    },
    "claude-chat": {
      "versions": {
        "^1.0.0": "consumeClaudeChat"
      }
    }
  },
  "dependencies": {
    "bytes": "^3.1.2",
    "etch": "^0.14.1"
  },
  "deserializers": {
    "ImageEditor": "deserialize"
  },
  "configSchema": {
    "defaultBackgroundColor": {
      "order": 1,
      "title": "Default Background Color",
      "type": "string",
      "enum": [
        "native",
        "white",
        "black",
        "transparent"
      ],
      "default": "native"
    },
    "autoSelectTolerance": {
      "order": 2,
      "title": "Auto-Select Tolerance",
      "description": "Color difference tolerance for auto-selection (0-255). Lower values require more similar colors to be considered background.",
      "type": "integer",
      "default": 30,
      "minimum": 0,
      "maximum": 255
    },
    "largeImageThreshold": {
      "order": 3,
      "title": "Large Image Threshold",
      "description": "File size threshold (in MB) for applying optimizations. Images larger than this will use async decoding and compressed history.",
      "type": "number",
      "default": 2,
      "minimum": 0.5,
      "maximum": 50
    },
    "maxHistorySize": {
      "order": 4,
      "title": "Maximum History Size",
      "description": "Maximum number of undo/redo states to keep (reduced automatically for large images).",
      "type": "integer",
      "default": 50,
      "minimum": 5,
      "maximum": 200
    },
    "wheelNavigationDelay": {
      "order": 5,
      "title": "Mouse Wheel Navigation Delay",
      "description": "Minimum delay (in milliseconds) between mouse wheel navigation events. Higher values prevent accidental rapid scrolling.",
      "type": "integer",
      "default": 150,
      "minimum": 50,
      "maximum": 500
    },
    "showSuccessMessages": {
      "order": 6,
      "title": "Show Success Messages",
      "description": "Show success notifications after operations like crop, transform, save, etc.",
      "type": "boolean",
      "default": true
    },
    "enablePreloading": {
      "order": 7,
      "title": "Enable Image Preloading",
      "description": "Preload adjacent images in the background for instant navigation. Uses more memory but makes navigation feel instant.",
      "type": "boolean",
      "default": true
    },
    "switchZoomAndNavigation": {
      "order": 8,
      "title": "Switch Zoom and Navigation",
      "description": "When enabled, scroll wheel zooms and Ctrl+scroll navigates. When disabled, scroll wheel navigates and Ctrl+scroll zooms.",
      "type": "boolean",
      "default": true
    },
    "autoZoomLimit": {
      "order": 9,
      "title": "Limit Auto Zoom to 100%",
      "description": "When enabled, auto zoom (fit to viewport) will not scale images beyond 100%. When disabled, small images will be scaled up to fit the viewport.",
      "type": "boolean",
      "default": true
    },
    "scrollCycle": {
      "order": 10,
      "title": "Cycle Through Images",
      "description": "When enabled, navigating past the last image wraps to the first image (and vice versa). When disabled, navigation stops at the boundaries.",
      "type": "boolean",
      "default": true
    }
  }
}
